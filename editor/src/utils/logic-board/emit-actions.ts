// ---------------------------------------------------------------------------
// Re-exports for action emission (sequence + guarded branches).
// ---------------------------------------------------------------------------

export { emitActionSequence } from './emit-action-sequence'
export { emitGuardedBranches } from './emit-guarded-branches'
export { emitGuardedActions } from './emit-guarded-branches'
