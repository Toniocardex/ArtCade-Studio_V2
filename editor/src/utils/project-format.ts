/** Current on-disk project schema revision (envelope authority — no imports). */
export const CURRENT_PROJECT_FORMAT_VERSION = 4

/** Legacy alias kept for object-type migration code. */
export const PROJECT_FORMAT_V4 = CURRENT_PROJECT_FORMAT_VERSION

/** Editor release string written on save; not a schema version. */
export const EDITOR_ENGINE_VERSION = '2.0.0'
