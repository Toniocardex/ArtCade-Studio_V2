// ---------------------------------------------------------------------------
// AssetBrowserPanel — thin re-export of the tabbed grid (legacy layout).
// Canvas mode uses ProjectExplorerPanel; import/remove logic lives in
// hooks/useAssetExplorerActions.ts (shared with the explorer tree).
// ---------------------------------------------------------------------------

export { default } from '../components/asset-explorer/AssetBrowserGridPanel'
