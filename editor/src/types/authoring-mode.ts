/** Editor presentation tier — same tools, different guidance/density (see LOGIC_BOARD_UX_CHARTER). */
export type AuthoringMode = 'base' | 'advanced'

export const AUTHORING_MODE_STORAGE_KEY = 'artcade.authoringMode'

export const DEFAULT_AUTHORING_MODE: AuthoringMode = 'base'
