# ArtCade Documentation

Current architecture: RmlUi (sibling ArtCade_Editor_RmlUi) presentation, C++ `ProjectDoc` and commands, and C++ runtime/export. Consult `.cursor/rules` and `AGENTS.md` for authoritative guidance.
