name: editor

on:
  push:
    branches: [main]
    paths:
      - 'editor/**'
      - 'package.json'
      - 'package-lock.json'
      - '.github/workflows/editor.yml'
  pull_request:
    paths:
      - 'editor/**'
      - 'package.json'
      - 'package-lock.json'
      - '.github/workflows/editor.yml'

jobs:
  ci:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Type-check
        run: npm -w editor run lint

      - name: Test
        run: npm -w editor test
